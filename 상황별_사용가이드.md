# AI PRONOTE v1.5 상황별 사용 가이드

## 1. 일반 회의 — 음성만

`녹음 시작` → `카메라 영상 함께 녹화` 끄기 → AI 선택 → 시작. 가장 가볍고 긴 회의에 적합합니다.

## 2. 칠판·회의실까지 남기기 — 영상+음성

`녹음 시작` → `카메라 영상 함께 녹화` 켜기 → 카메라가 칠판을 향하는지 미리 확인 → 시작. 영상은 로컬 라이브러리에 저장되고, 동시에 녹음된 음성으로 받아쓰기와 회의록을 만듭니다.

## 3. 녹음 없이 필기만

홈의 `필기만 시작`을 누릅니다. 키보드 노트와 펜 필기를 사용할 수 있으며 녹음이나 AI 전송은 시작되지 않습니다.

## 4. 예전에 녹음한 파일

홈의 `파일로 회의록 만들기`를 누르고 MP3, WAV, M4A, WEBM, OGG, FLAC 또는 MP4 파일 하나를 고릅니다. 최대 크기는 512MB입니다. 결과 형식과 제목·참석자를 확인한 뒤 `받아쓰기·회의록 만들기`를 누릅니다. 모르면 기본값인 `회의록`을 선택하세요. 진행은 오른쪽 아래 `작업함`, 완료 결과는 `라이브러리`에서 확인합니다.

파일 가져오기는 기존 녹음·영상으로 회의록을 만드는 부가기능이며, 기본 흐름은 앱에서 바로 회의를 녹음하는 것입니다.

## 5. AI 선택

- 공식 API(BYOK): 설정에서 OpenAI, Gemini 또는 Anthropic API 키를 연결하는 정식 방식
- Claude CLI: Claude 로그인 상태를 이용하는 비공개 베타용 실험 연결
- Gemini CLI: Google 로그인 상태를 이용하는 실험 연결(계정·CLI 이용 자격 필요)
- Codex CLI: Codex CLI 로그인 상태를 이용하는 실험 연결. 일반 ChatGPT 웹 로그인과 동일하다고 보장하지 않습니다.

설정에서 실제로 연결한 AI를 선택합니다. CLI 로그인 자격정보는 AI PRONOTE가 복사하거나 저장하지 않습니다. AI 회의록이 필요 없으면 연결하지 않고 로컬 녹음·필기만 사용할 수 있습니다.

## 6. 회의록 다시 만들기

받아쓰기는 끝났지만 AI 정리만 실패했다면 인터넷 연결과 해당 AI의 로그인·사용량을 확인한 뒤 작업함에서 `회의록 다시 만들기`를 누릅니다.

## 7. 파일을 잘못 선택했을 때

문서 형식 선택 창에서 `취소`하면 받아쓰기를 시작하지 않습니다. 가져온 원본은 라이브러리에 보관됩니다. 민감 파일을 잘못 골랐다면 라이브러리에서 해당 항목의 삭제 메뉴를 바로 사용하세요.

## 8. 개인정보가 중요한 회의

참석자 동의를 먼저 받고, 회사의 보안 정책상 외부 AI 전송이 허용되는지 확인합니다. 허용되지 않으면 AI 회의록을 끄고 로컬 녹음·필기만 사용합니다.
