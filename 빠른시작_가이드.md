# AI PRONOTE v1.5 빠른 시작

녹음·녹화와 외부 AI 처리는 지역별 법규와 조직 정책이 다를 수 있습니다. 사용 환경에 적용되는 기준을 확인한 뒤 필요한 절차를 따르세요.

> **AI 연결 없이 바로 시작하세요.** 녹음·영상·받아쓰기·필기는 그대로 사용할 수 있습니다. AI 회의록, 받아쓰기 정확도 향상, 에이전트 기능이 필요할 때 AI를 연결하세요.

휴대폰·iPad에서는 PC와 같은 Wi-Fi에 연결한 뒤 PC가 표시한 QR과 일회용 연결 코드를 사용합니다. 자세한 사용법은 `docs/휴대폰_iPad_사용가이드.md`를 참고하세요.

## 처음 한 번만

### Mac

Apple Silicon(M1 이상) Mac, 인터넷 연결, Python 3.12가 필요합니다. Python이 없거나 버전·CPU가 맞지 않으면 첫 설정이 공식 설치 페이지를 열고 종료합니다. Python 3.12 설치 후 `AI_PRONOTE_설치하기.command`를 다시 실행하세요. 이번 베타는 Intel Mac 설치를 보장하지 않습니다.

1. ZIP을 `문서/AI PRONOTE` 같은 로컬 폴더에 완전히 풉니다.
2. `AI_PRONOTE_설치하기.command`를 Control-클릭 → 열기 합니다. 처음 설치는 인터넷 속도에 따라 수 분 걸릴 수 있습니다.
3. AI 회의록·더 정확한 받아쓰기·에이전트 기능을 쓸 경우에만 연결합니다. Claude 또는 ChatGPT는 `mac/2_AI_LOGIN.command`에서 로그인하고, Gemini는 앱 설정에서 Gemini API 키를 연결합니다.
4. `mac/AI PRONOTE.app`을 Control-클릭 → 열기 합니다.
5. 브라우저가 마이크·카메라 사용을 물으면 허용합니다.

### Windows

Windows 10/11 64비트와 Python 3.12 64비트가 필요합니다.

1. ZIP을 `문서\AI PRONOTE` 같은 로컬 폴더에 완전히 풉니다.
2. `AI_PRONOTE_설치하기.cmd`를 더블클릭합니다.
3. AI 회의록·더 정확한 받아쓰기·에이전트 기능을 쓸 경우에만 연결합니다. Claude 또는 ChatGPT는 `2_AI_LOGIN.cmd`에서 로그인하고, Gemini는 앱 설정에서 Gemini API 키를 연결합니다.
4. `3_START_AI_PRONOTE.vbs`를 실행합니다.

## 회의 시작

1. 홈의 `녹음 시작`을 누릅니다.
2. 음성만 필요하면 `음성 녹음`을 선택합니다.
3. 회의실, 참석자 또는 칠판까지 남기려면 `영상 녹화`를 선택합니다.
4. AI 기능을 사용할 경우 선택한 외부 AI로 내용이 전송될 수 있음을 확인하고 시작합니다.
5. 종료하면 음성 받아쓰기와 회의록 생성이 이어집니다. 영상 모드에서도 회의록이 생성됩니다.

## 기존 녹음·영상 파일로 회의록 만들기

1. 홈에서 `파일로 회의록 만들기`를 누릅니다.
2. MP3, WAV, M4A, WEBM, OGG, FLAC 또는 MP4 파일 하나를 선택합니다. 최대 크기는 512MB입니다.
3. `회의록`, `강의 노트`, `대화 정리` 등 원하는 문서 형식을 고릅니다. 잘 모르겠으면 기본값인 `회의록`을 선택합니다.
4. 필요하면 제목과 참석자를 입력하고 `받아쓰기·회의록 만들기`를 누릅니다.
5. 진행 상황은 오른쪽 아래 `작업함`에서 확인합니다. 창을 닫았다 다시 열어도 작업 상태를 이어서 확인할 수 있습니다.
6. 완료된 결과는 `라이브러리`에서 다시 열고 원본을 재생하거나 내려받을 수 있습니다.

한 번에 파일 하나씩 처리하면 제목과 결과 형식을 잘못 연결하는 일을 줄일 수 있습니다.

## 문제 발생 시

- Claude 또는 ChatGPT/Codex 로그인이 만료되면 `2_AI_LOGIN`을 다시 실행합니다.
- Gemini 연결에 문제가 있으면 앱 설정에 입력한 API 키와 사용량을 확인합니다.
- 프로그램을 연속으로 여러 번 실행하지 말고 45초 기다린 뒤 한 번만 다시 엽니다.
- 오류를 전달할 때 회의 내용, 로그인 정보, 토큰은 보내지 말고 화면과 작업 상태만 캡처합니다.
- 원본 녹음·영상 파일은 확인이 끝날 때까지 지우지 않습니다.
- 파일 선택 후 아무 변화가 없으면 512MB 이하의 지원 형식인지 확인하고 다시 선택합니다.
