# AI PRONOTE v1.5 빠른 시작

이 배포본은 지정 사용자용 코드 서명 전 비공개 베타입니다. 실제 회의 전에 참석자에게 녹음·녹화 및 AI 처리 사실을 알리고 동의를 받으세요.

## 처음 한 번만

### Mac

1. ZIP을 `문서/AI PRONOTE` 같은 로컬 폴더에 완전히 풉니다.
2. `mac/1_FIRST_SETUP.command`를 Control-클릭 → 열기 합니다.
3. `mac/2_AI_LOGIN.command`에서 Claude, Gemini 또는 ChatGPT/Codex에 로그인합니다.
4. `mac/AI PRONOTE.app`을 Control-클릭 → 열기 합니다.
5. 브라우저가 마이크·카메라 사용을 물으면 허용합니다.

### Windows

1. ZIP을 `문서\AI PRONOTE` 같은 로컬 폴더에 완전히 풉니다.
2. `1_FIRST_SETUP.cmd`를 실행합니다.
3. `2_AI_LOGIN.cmd`에서 사용할 AI에 로그인합니다.
4. `3_START_AI_PRONOTE.vbs`를 실행합니다.

## 회의 시작

1. 홈의 `녹음 시작`을 누릅니다.
2. 음성만 필요하면 `카메라 영상 함께 녹화`를 끕니다.
3. 회의실, 참석자 또는 칠판까지 남기려면 이를 켭니다.
4. 사용할 AI와 외부 AI 전송 동의를 확인하고 시작합니다.
5. 종료하면 음성 받아쓰기와 회의록 생성이 이어집니다. 영상 모드에서도 회의록이 생성됩니다.

## 문제 발생 시

- AI 로그인이 만료되면 `2_AI_LOGIN`을 다시 실행합니다.
- Gemini는 Google 계정 종류와 현재 Gemini CLI 이용 정책에 따라 로그인이 거절될 수 있습니다. 이 경우 Claude 또는 ChatGPT/Codex를 선택합니다.
- 프로그램을 연속으로 여러 번 실행하지 말고 45초 기다린 뒤 한 번만 다시 엽니다.
- 오류를 전달할 때 회의 내용, 로그인 정보, 토큰은 보내지 말고 화면과 작업 상태만 캡처합니다.
- 원본 녹음·영상 파일은 확인이 끝날 때까지 지우지 않습니다.
