# AI PRONOTE v1.5 빠른 시작

이 배포본은 지정 사용자용 코드 서명 전 비공개 베타입니다. 실제 회의 전에 참석자에게 녹음·녹화 및 AI 처리 사실을 알리고 동의를 받으세요.

## 처음 한 번만

### Mac

인터넷 연결과 Python 3.10~3.12가 필요합니다. Python이 없으면 첫 설정이 공식 설치 페이지를 열고 종료합니다. Python 설치 후 `1_FIRST_SETUP.command`를 다시 실행하세요.

1. ZIP을 `문서/AI PRONOTE` 같은 로컬 폴더에 완전히 풉니다.
2. `mac/1_FIRST_SETUP.command`를 Control-클릭 → 열기 합니다. 처음 설치는 인터넷 속도에 따라 수 분 걸릴 수 있습니다.
3. AI 회의록을 쓸 경우 `mac/2_AI_LOGIN.command`에서 Claude, Gemini 또는 Codex CLI에 로그인합니다. 현재 로그인 방식은 비공개 베타용 **실험적 CLI 연결**이며, 설정의 공식 API(BYOK)와 별개입니다. 로컬 녹음·필기만 쓸 때는 이 단계를 건너뛸 수 있습니다.
4. `mac/AI PRONOTE.app`을 Control-클릭 → 열기 합니다.
5. 브라우저가 마이크·카메라 사용을 물으면 허용합니다.

### Windows

1. ZIP을 `문서\AI PRONOTE` 같은 로컬 폴더에 완전히 풉니다.
2. `1_FIRST_SETUP.cmd`를 실행합니다.
3. AI 회의록을 쓸 경우 `2_AI_LOGIN.cmd`에서 사용할 Claude, Gemini 또는 Codex CLI에 로그인합니다. 이는 비공개 베타용 실험 연결이며, 로컬 녹음·필기만 쓸 때는 건너뛸 수 있습니다.
4. `3_START_AI_PRONOTE.vbs`를 실행합니다.

## 회의 시작

1. 홈의 `녹음 시작`을 누릅니다.
2. 음성만 필요하면 `카메라 영상 함께 녹화`를 끕니다.
3. 회의실, 참석자 또는 칠판까지 남기려면 이를 켭니다.
4. 사용할 AI와 외부 AI 전송 동의를 확인하고 시작합니다.
5. 종료하면 음성 받아쓰기와 회의록 생성이 이어집니다. 영상 모드에서도 회의록이 생성됩니다.

## 기존 녹음·영상 파일로 회의록 만들기

1. 홈에서 `파일로 회의록 만들기`를 누릅니다.
2. MP3, WAV, M4A, WEBM, OGG, FLAC 또는 MP4 파일 하나를 선택합니다. 최대 크기는 512MB입니다.
3. 원하는 문서 형식을 고릅니다. 잘 모르겠으면 기본값인 `회의록`을 선택합니다.
4. 제목과 참석자를 확인하고 `받아쓰기·회의록 만들기`를 누릅니다.
5. 진행 상황은 오른쪽 아래 `작업함`, 완료 결과는 `라이브러리`에서 확인합니다.

한 번에 파일 하나씩 처리하면 제목과 결과 형식을 잘못 연결하는 일을 줄일 수 있습니다.

## 문제 발생 시

- AI 로그인이 만료되면 `2_AI_LOGIN`을 다시 실행합니다.
- Gemini는 Google 계정 종류와 현재 Gemini CLI 이용 정책에 따라 로그인이 거절될 수 있습니다. 이 경우 Claude CLI, Codex CLI 또는 설정의 공식 API(BYOK)를 선택합니다.
- 프로그램을 연속으로 여러 번 실행하지 말고 45초 기다린 뒤 한 번만 다시 엽니다.
- 오류를 전달할 때 회의 내용, 로그인 정보, 토큰은 보내지 말고 화면과 작업 상태만 캡처합니다.
- 원본 녹음·영상 파일은 확인이 끝날 때까지 지우지 않습니다.
- 파일 선택 후 아무 변화가 없으면 512MB 이하의 지원 형식인지 확인하고 다시 선택합니다.
